以下、あなたが挙げた要件（災害マップの核＋速報＋区画検索＋UX＋性能）を **同時に実装できるように落とした仕様書（ドラフト）**。
実装チケットに切れる粒度まで分解してあります。

---

# SVGMap 災害マップ 仕様書（ドラフト v0.9）

## 0. 目的・スコープ

### 目的

* 地図上で災害リスク（浸水・土砂・津波）と避難行動（避難所等）を重ね、**クリック地点のリスクを即時提示**する。
* 同時に **速報（警報・停電・道路規制・河川水位等）**をタイムラインで表示し、地図上のピンと連動させる。
* 区画（町丁目等）単位での理解を支援する（境界表示＋検索＋ハイライト）。

### 対象ユーザー

* デモ参加者（一般）／研究室内利用／自治体オープンデータ活用のPoC

### 非スコープ（v1ではやらない）

* ルート案内の本格実装（徒歩時間は概算のみ）
* 避難所のリアルタイム開設状況（データがあれば後付け）
* 公式APIの完全網羅（RSS/CSV/JSON中心に開始）

---

## 1. システム構成（概略）

* フロントエンド：SVGMap表示（既存）＋UI（左サイド/右パネル/下タイムライン）
* データ供給：

  * 静的データ：GeoJSON（ハザード面/行政界/避難所点等）
  * 動的データ：RSS/JSON（速報）
* 位置→区画判定：

  * v1：町丁目境界 GeoJSON に対する point-in-polygon（PIP）
  * フォールバック：逆ジオコーディング（任意）

---

## 2. データ仕様

### 2.1 レイヤ一覧

#### Hazard（面）

1. 浸水想定区域（深さ別）
2. 土砂災害警戒区域（警戒/特別警戒）
3. 津波浸水想定（沿岸のみ）
4. 規模切替（想定最大規模 / 計画規模） ※データがある場合

#### Evac/Facilities（点）

* 避難所（施設種別・収容）
* 給水拠点・防災倉庫（任意）
* 病院・消防・警察（ランドマーク）

#### Boundary（面/線）

* 町丁目境界（e-Stat等）
* 市区町村境界（任意：速度改善の二段判定にも利用）

#### Alerts/News（点）

* 警報・注意報（気象）
* 停電・道路規制・河川水位（RSS/オープンデータ）
* 地震・津波・台風等（拡張）

---

### 2.2 GeoJSON 共通フィールド（必須）

* `source_name`：出典名（例：国交省、岡山市など）
* `source_url`：出典URL（生URLでも可）
* `updated_at`：データ更新日（YYYY-MM-DD）
* `license`：ライセンス表記（CC BY等）
* `layer_type`：`hazard_flood` / `hazard_landslide` / `evac_shelter` …など

### 2.3 レイヤ個別フィールド（例）

#### 浸水（hazard_flood）

* `depth_m`（数値 or 区分コード）
* `depth_class`（例：`lt0_5`, `0_5to3`, `3to5`, `gt5`）
* `scenario`：`max` / `plan`（規模）

#### 土砂（hazard_landslide）

* `hazard_kind`（急傾斜/土石流/地すべり）
* `level`：`warning` / `special_warning`

#### 津波（hazard_tsunami）

* `depth_class`（浸水に準じる）
* `scenario`：`max` / `plan`

#### 避難所（evac_shelter）

* `name`
* `type`（指定緊急避難場所/指定避難所等）
* `capacity`（任意）
* `address`（任意）
* `lat`, `lon`（Point geometryなら不要）

#### 行政界（boundary_chome）

* `KEY_CODE`（e-Statのキー）
* `PREF_NAME`, `CITY_NAME`, `S_NAME`
* `PREF`, `CITY` 等（コード）

---

## 3. 表示仕様（スタイル・凡例）

### 3.1 凡例（Legend）

* Hazardは **カテゴリに対して固定色**（深さ/危険度）
* 土砂は `warning` と `special_warning` を色で明確に分離
* 透明度（Opacity）スライダー：各レイヤグループごと（Hazard/Boundary）

### 3.2 表示優先順位（重なり）

1. 選択ハイライト（クリック地点/対象ポリゴン）
2. 速報ピン
3. 避難所点
4. Hazard面
5. 境界線（薄く）

---

## 4. UI仕様

## 4.1 画面レイアウト

* 左パネル：レイヤ一覧（Hazard/避難/速報/境界）＋検索＋凡例
* 中央：地図
* 右パネル：リスクカード＋出典/更新情報＋“この地点の区画名”
* 下パネル：速報タイムライン（地図ピンと連動）

### 4.2 左パネル（Layer Control）

* レイヤトグル（ON/OFF）
* グループ単位トグル（HazardまとめてON等）
* 透明度スライダー（Hazard/Boundary）
* 凡例表示（ON中のHazardに応じて自動切替）
* 検索（住所/町丁目/施設名）

### 4.3 右パネル（Risk Card）

#### 表示トリガ

* 地図クリック
* 検索結果選択
* タイムライン項目選択（任意：その地点へフォーカス）

#### リスクカード表示項目（必須）

* 位置：緯度経度（小さく）
* 区画名：`CITY_NAME + S_NAME`（取れない場合は市区町村）
* 浸水：深さカテゴリ（該当なしなら「対象外」）
* 土砂：該当/非該当＋種別
* 津波：該当/非該当（沿岸のみ）
* 近い避難所：上位N件（デフォルト3）

  * 距離（直線距離）
  * 徒歩時間概算（後述）
* 標高：値（任意：データがある場合）
* 出典・更新日・注意書き（レイヤごとに明示）

#### 徒歩時間概算（仕様）

* 直線距離 `d(m)` を用い、徒歩速度 80 m/min（= 4.8 km/h）で `t = ceil(d / 80)` 分
* 注意：道路距離ではない旨を明記

### 4.4 下パネル（Timeline）

* アイテム表示：`時刻 / 種別 / 地域 / タイトル`
* フィルタ：種別（気象/停電/道路/河川…）
* クリック：

  * 地図にピン表示
  * 右パネルに詳細（本文/リンク/出典）

---

## 5. 機能仕様（ロジック）

### 5.1 クリック地点の評価フロー

1. クリック座標 `P` 取得
2. 区画判定（boundary_chome）
3. Hazard判定

   * 浸水ポリゴンに包含されるか（最大/計画の選択状態に応じる）
   * 土砂ポリゴンに包含されるか
   * 津波ポリゴンに包含されるか（該当レイヤON時）
4. 近傍施設検索（避難所など）
5. リスクカード更新＋ハイライト描画

### 5.2 point-in-polygon（PIP）最適化仕様

* 全ポリゴン走査は禁止（重い）
* **候補絞り込み**を必須化：

  * (A) bboxフィルタ：各featureに `bbox`（minx,miny,maxx,maxy）保持
  * (B) グリッドインデックス：ズーム固定（例：z=12）でセルID→featureID配列
* 手順：

  1. `cellId(P)` から候補 featureIDs を取得
  2. bbox内のものだけ PIP
  3. 最初にヒットしたもの or 最適（ルール）を採用

### 5.3 近傍施設検索

* 施設点に対して、同様にグリッド/タイルで候補絞り込み
* 直線距離で上位N件
* 施設の種類フィルタ（避難所だけ / 全施設）をUIで選べると良い（v1は避難所のみでもOK）

### 5.4 規模切替（想定最大/計画）

* UI：トグル（max/plan）
* 表示：該当scenarioのポリゴンだけ描画
* 判定：PIPも同様にscenarioフィルタ適用

---

## 6. 速報（ニュース/アラート）仕様

### 6.1 データモデル（共通）

* `id`
* `published_at`（ISO8601）
* `type`（weather/power/road/river/quake…）
* `title`
* `summary`（短文）
* `source_name`
* `source_url`
* `area_text`（地域表現：岡山市北区 など）
* `location`（Point：緯度経度が取れる場合）
* `raw`（任意：原文）

### 6.2 取得・更新

* ポーリング周期：デフォルト 5分（設定可能）
* 新規/更新の判定：`id` または `source_url` で重複排除
* 表示件数：最新 50（種別フィルタで絞る）

### 6.3 地域紐付け

* 位置情報がある場合：その座標をピン
* 位置がない場合：

  * `area_text` を表示するのみ（地図ピンは出さない、または代表点に置く）
  * 将来：区画コードと紐付け（KEY_CODE等）できるなら強化

---

## 7. URL共有（状態保存）仕様

### 7.1 保存対象

* 中心座標 `lat,lon`
* ズーム `z`
* レイヤON/OFF状態（ビット列 or JSON圧縮）
* 透明度（Hazard/Boundary）
* 規模（max/plan）
* 選択地点（あれば）

### 7.2 形式

* `#` フラグメント or クエリにbase64圧縮JSON
* 例：`#s=<compressed>`
  （実装簡易性を優先）

---

## 8. パフォーマンス要件

### 8.1 目標

* 初回表示：5秒以内（PC想定）
* クリック→リスクカード更新：200ms〜500ms程度（体感“即時”）
* スクロール/ズーム：引っかかりが許容範囲

### 8.2 データ量対策

* **ズーム連動の簡略化**（必須）

  * 低ズーム：simplified GeoJSON
  * 高ズーム：full GeoJSON
* 切替閾値：例 z<13 で簡略、z>=13 で詳細（調整可）
* 19MB級は **初期ロードで一括読込しない**（ズーム/範囲で段階ロード推奨）

### 8.3 デバッグ可視化

* 画面右上に表示：

  * 現在描画中 feature数（レイヤ別）
  * クリック判定の候補数（PIP前/後）
  * 処理時間（ms）

---

## 9. エラーハンドリング・注意表示

* データ未取得時：レイヤ名に警告アイコン＋理由（CORS、404等）
* 速報が空：粒度を上げて検索…は今回「外部ニュース検索」は未実装なので、空を正しく表示
* 注意書き（固定）：

  * 想定は最大浸水等であり実際と異なる可能性
  * 徒歩時間は直線距離概算
  * データ更新日の明示

---

## 10. 受け入れ基準（Acceptance Criteria）

### Hazard

* レイヤON/OFF、凡例、透明度が動作
* 規模切替で表示・判定が切り替わる
* クリック地点で浸水/土砂/津波の該当が正しく出る（サンプル地点で検証）

### Evac

* 避難所が表示され、クリックで属性表示
* リスクカードで最寄り避難所が3件表示され距離が妥当

### Boundary/Search

* 町丁目境界が表示可能
* 検索で該当エリアへズーム＆ハイライト

### Alerts

* 速報一覧がタイムラインに時系列で表示
* クリックで詳細と（座標があるものは）ピン表示

### 性能

* PIPが「グリッド＋bbox」経由になっており、ポリゴン全走査になっていない
* 右上デバッグ表示で候補数・時間が確認できる

---

## 11. 実装チケット分割（おすすめ順）

1. **レイヤ管理＋凡例＋透明度**（UI骨格）
2. **ハザード（浸水/土砂/津波）表示**（静的ロード）
3. **クリック→リスクカード（まずは浸水だけ）**
4. **PIP最適化（bbox＋グリッド）**
5. **避難所表示＋最寄り3件計算**
6. **町丁目境界表示＋区画判定（クリックで区画名表示）**
7. **検索（町丁目/施設）→ズーム＆ハイライト**
8. **速報タイムライン（RSS/JSON）＋ピン連動**
9. **ズーム連動の簡略化（簡略/詳細切替）**
10. **URL共有（状態保存）**
11. **デバッグ可視化（feature数・処理時間）**

---

## 12. 未確定事項（実装しながら決めるが、初期値は置く）

* ズーム閾値（簡略/詳細）：初期 z=13
* グリッド解像度：WebMercator z=12相当
* 速報の情報源（どのRSS/オープンデータを採用するか）
* 標高データ（入れるならどのソースか）

---

必要なら、この仕様を **そのままREADME + 実装TODO（チェックリスト）**に整形して、ディレクトリ構成案（`data/`, `layers/`, `ui/`, `index/` など）と、JSONスキーマ（TypeScript型）まで起こす。
今のSVGMap実装は JS/TS どっち？（どっちでも進められるけど、型を切るならTSが楽。）
